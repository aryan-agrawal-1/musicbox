import { View } from 'react-native';
import { Colors } from '@/constants/colors';

// P3.2 — Login screen (implemented in Phase 3)
export default function LoginScreen() {
  return <View style={{ flex: 1, backgroundColor: Colors.background }} />;
}
