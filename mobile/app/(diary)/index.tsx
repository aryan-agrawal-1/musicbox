import { View } from 'react-native';
import { Colors } from '@/constants/colors';

// P12.1 — Diary screen (implemented in Phase 12)
export default function DiaryScreen() {
  return <View style={{ flex: 1, backgroundColor: Colors.background }} />;
}
