import { View } from 'react-native';
import { Colors } from '@/constants/colors';

// P6.1 — Search screen (implemented in Phase 6)
export default function SearchScreen() {
  return <View style={{ flex: 1, backgroundColor: Colors.background }} />;
}
